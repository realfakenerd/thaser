import { Scene } from '@thaser/scene';
import {describe, it} from 'vitest';
import {Clock} from '..';
describe('/time Clock', () => {

    const clock = new Clock(new Scene({}))
    it('should start a clock', () => {

    })
})